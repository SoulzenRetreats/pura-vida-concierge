import { VendorsPageContent } from "@/components/vendors/VendorsPageContent";

export default function AdminVendors() {
  return (
    <VendorsPageContent
      titleKey="admin.vendors.title"
      descriptionKey="admin.vendors.description"
    />
  );
}
